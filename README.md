# Sistema POS para Librería (LibrarySystem)

Este proyecto es un Sistema de Punto de Venta (POS) desarrollado en Python con CustomTkinter y SQLite.

---

## Guía Rápida de Comandos de Git

Estos son los comandos para guardar y subir cambios al repositorio de GitHub.

### Flujo de Trabajo (Para subir cambios nuevos)

Sigue estos 3 pasos CADA VEZ que quieras guardar un avance (ej: al final del día o al terminar una función).

#### 1. Preparar los cambios

Este comando revisa todos los archivos que modificaste o creaste y los prepara para "guardarlos".

```bash
git add .

git commit -m "Escribe tu mensaje descriptivo aquí"

git push